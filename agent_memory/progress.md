# 当前任务进度

## 目标

交付“序升”第一版可运行 PWA 原型，完成五页信息架构和核心经验成长闭环。

## 当前阶段

- [x] 明确 MVP 范围、成功标准和设计方向。
- [x] 建立产品与设计基线。
- [x] 实现 PWA 五页、成长循环、本地存储和离线壳。
- [x] 验证关键行为、PWA 配置与移动端布局。
- [x] 首页完成暖白测绘纸 × 炭黑行动舱 × 荧光黄信号的视觉替换并通过最终审查。
- [x] 更新 `DESIGN.md` 与 `.impeccable/design.json`，记录五页统一的暖白测绘纸 × 炭黑行动舱 × 荧光黄系统，并明确首页海报式构图与其余四页滚动档案布局的职责差异。
- [x] 首页眨眼从约 192ms 调整为约 320ms，增加半闭与闭眼停留，降低闪帧感。
- [x] 首页竖向进度尺从 `left:42%` 移至 `36%`，避开人物眼睛和头发区域。
- [x] 依次迁移主线、角色、成就、支线页面到暖白测绘纸 × 炭黑行动舱 × 荧光黄系统。
- [x] 角色页先完成 `body_full_v3.png` 全身补齐，随后已由一体化素材 `profile_character_integrated_v2.png` 替代；390×844 仍从狼耳到双脚完整显示。
- [x] 将力量、生命、智慧、魅力从数据舱迁移到人物四周的非对称扫描 HUD；390×844 下四个节点均未越界，人物脸部、狼耳与双脚保持无遮挡。
- [x] 角色页改用一体化全身立绘 `profile_character_integrated_v2.png`，解决旧头颈图层与补全身体比例不一致；移除穿过头发的舞台顶线。
- [x] 首页替换为干净头颅 `cranium_earless_refined_v2.png`，并用原头颅素材作为 CSS mask 锁定轮廓与坐标；清除灰紫脏斑，同时保留狼耳和眨眼分层。
- [x] 半闭与全闭眨眼帧改为同一干净脸版本，并分别用旧帧 alpha 轮廓锁定位置；眨眼不再闪回旧脏脸或旧色调。
- [x] 主线从硬编码任务迁移为可添加、编辑、停用的本地任务档案，并接入固定经验档位。
- [x] 成就中心新增“脂域核心 · 15”讨伐入口与二级页面，包含 15 个节点、三阶段奖励、本周行动护盾和进度日志。
- [x] 成就里程碑扩展为 7 / 21 / 30 天连续节点、属性累计、全维同步、知识采样和 Boss 节点。
- [x] 首页头部组围绕既有颈根锚点缩至 96% 并上移 7px，增加颈部露出、修正头肩比例；眨眼和双耳继续随同一头部组运动。

## 验证结果

- `.impeccable/design.json` 已通过 PowerShell `ConvertFrom-Json` 解析（schemaVersion 2，8 个组件）；`git diff --check` 无报错。
- `app.js`、`sw.js` 通过 Node 语法检查，manifest 可解析。
- 首页 Live2D 接入后，`app.js` 与 `sw.js` 再次通过 Node 语法检查；Service Worker 清单中的 8 个分层素材均存在。
- 390×844 手机视口实测：首页、主线、角色页无横向溢出。
- 完成健身单车后：0 → 40 XP、完成度 0% → 14%、力量 0 → 1。
- 刷新后累计 40 XP 仍保留，证明 localStorage 持久化正常。
- 页面控制台无 error / warning；设计检测器返回空结果。
- 本轮 `app.js`、`sw.js` 已通过 Node 语法检查，HTML 中脚本引用的固定 ID 均存在，`git diff --check` 无格式错误。
- 本轮设计检测器仅报告 2 处进度条宽度过渡警告和既有 / 少量色值提示；未发现结构性反模式。由于当前环境无界面浏览器启动失败，本轮手机交互回归尚未完成，需在下一次可用浏览器或真机预览时补测。

## 下一阶段建议

优先深化健康模块：为服药 / 涂药加入可配置计划、单项连续天数与恢复备注，不先扩张更多页面。

## 本轮增量

- [x] 生成原创“序升者”角色主立绘并保存到 `assets/sequence-avatar-v1.png`。
- [x] 首页改为角色舱主视图，等级、经验与同步率围绕人物呈现。
- [x] 离线缓存更新至 `xusheng-v4` 并纳入角色图片。
- [x] 完成狼卫 Live2D 分层设定板 V1，明确脸部、发型、狼耳、身体、服装、装甲、手部、披风和尾巴的独立图层。
- [ ] 按设定板生产透明分层素材并进行 Live2D 网格绑定。
- [x] 生成狼卫 LV.01 最终半身 Live2D 身份母版 V1，等待人物身份与气质确认。
- [x] 完成首个真实 Live2D 透明图层 `face_base.png`，包含补画后的额头、太阳穴、耳朵、眼窝、下颌与颈部隐藏区域。
- [x] 完成左右眼白、琥珀瞳、上下眼睑、眉毛、闭嘴与张嘴口腔共 12 个独立透明图层。
- [x] 完成后发、前刘海、左右侧发、左右狼耳外层与内层共 8 个独立透明图层，并清理跨网格漂浮碎片。
- [x] 完成颈部、躯干底层、左右上臂、左右前臂护具与左右手套共 8 个透明图层，并统一清理跨网格碎片。
- [x] 完成左右肩甲结构、胸前绑带、金属配件、腰带与左右髋部绑带共 8 个透明覆盖图层，并保留胸前多零件结构。
- [x] 完成前后披风、左右衣摆和三段尾巴共 8 个透明物理图层，并清理漂浮像素。
- [x] 建立统一画布、图层顺序、参数组清单与交互式组装预览。
- [ ] 组装验证未通过：分批生成的小图层比例、透视与坐标不一致，不能直接进入 Live2D 绑定。
- [x] 改用同一母版原位切层：已完成 1024×1536 身体层、头部层和前景高领层。
- [x] 按耳尖、下巴、肩线锚点校正头部内容比例，零偏移装配无重影，颈部接缝通过。
- [x] 新增 `design-concepts/live2d/aligned/live2d-proof.html`，验证身体呼吸与头部独立微动。
- [x] 修复头部运动时下巴被横向切断：前景高领由矩形裁片改为带中央 U 形安全区的轮廓遮罩。
- [x] 修复头身运动不同步：头部继承身体呼吸位移；由于当前头部大层包含整段脖子，暂时关闭独立摆头，避免皮肤与衣领相对滑动。
- [x] 将头颅与颈部分离：新增 `cranium_master_aligned.png` 与 `neck_body_aligned.png`，头颅围绕颈根微旋转，颈部固定随躯干呼吸。
- [x] 清理新头颅与颈部图层的绿色抠图色溢，并为动态页资源增加版本参数避免旧缓存。
- [x] 新增同位闭眼头颅帧 `cranium_blink_aligned.png`，使用 `step-end` 瞬时切换实现眨眼，避免透明渐变造成双脸重影。
- [x] 输出闭眼装配验证图 `assembly_blink_preview.jpg`，头身比例与颈部接缝通过。
- [x] 重做眨眼为五段状态：睁眼 → 半闭 → 全闭 → 半闭 → 睁眼。
- [x] 动画只替换左右眼周羽化补丁，不再替换整张头颅，避免鼻子、嘴和脸型闪动。
- [x] 输出半闭眼验证图 `assembly_halfblink_preview.jpg`。
- [x] 眨眼由固定 CSS 周期改为随机 3.5–7.5 秒触发，并加入 16% 概率的快速双眨；半闭与全闭时长分别细化到毫秒级。
- [x] 新增无耳头颅底图，并从原位头颅提取左右狼耳透明层，耳朵移动时不会残留第二对耳朵。
- [x] 狼耳改为随机 4.8–11.8 秒触发单侧轻抖，左右耳使用镜像角度且动作不持续循环。
- [x] 输出狼耳装配验证图 `assembly_ears_preview.jpg`。
- [x] 新增点击 / 轻触角色回应：即时眨眼、左右狼耳错峰轻抖、HUD 信号确认与短促扫描框反馈。
- [x] 交互反馈不改变头颈位置，可直接作为后续“任务完成 / XP 入账”的角色响应基础。
- [x] 修复拆耳后眨眼错位：半闭与全闭状态全部基于当前无耳头颅重新生成，不再混用旧脸。
- [x] 眼周补丁遮罩缩窄至眉下与颧骨上方，并降低羽化范围，避免眉毛、鼻梁和脸型随眨眼跳动。
- [x] 点击回应不再必定眨眼，改为 22% 概率；普通点击主要触发随机单侧狼耳回应。
- [x] 移除整圈扫描边框，改为角色背后的暗红径向光晕，并关闭 iOS 点击高亮、焦点轮廓与图片拖拽。
- [x] 将验证通过的 Live2D 分层角色正式接入首页，替换静态 `sequence-avatar-v1.png`。
- [x] 首页任务完成后触发一次克制的角色奖励反馈：青色能量光晕、随机单耳回应，并以 28% 概率眨眼。
- [x] 首页轻触角色以单耳反馈为主，眨眼概率进一步降为 16%；自动眨眼与随机耳动保留。
- [x] Service Worker 缓存升级为 `xusheng-v5` 并纳入全部正式 Live2D 素材。
- [ ] 下一步在已对齐头部组内拆分眼睑、瞳孔、嘴部和左右狼耳，并逐组验证。

## 停止条件

- 五个页面均可从底部导航进入。
- 每日任务可完成 / 撤销，经验、属性和完成度同步变化。
- 支线可新增记录，状态刷新后保留。
- 成就和角色数据能由当前记录推导展示。
- PWA manifest 与 service worker 可用，关键脚本无语法错误。
