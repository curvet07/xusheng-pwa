---
name: "序升"
description: "暖白测绘纸、炭黑行动舱与荧光黄信号构成的个人行动档案。"
colors:
  paper: "#ececeb"
  paper-raised: "#f5f5f2"
  ink: "#111214"
  chamber: "#17191a"
  chamber-soft: "#222425"
  signal: "#e8f300"
  text-on-dark: "#f1f1ed"
  muted: "#626463"
  muted-on-dark: "#a9abaa"
  line: "#bfc0bd"
  risk: "#ff6b67"
typography:
  display:
    fontFamily: "Bahnschrift Condensed, Arial Narrow, sans-serif"
    fontWeight: 900
    lineHeight: 0.95
    letterSpacing: "-0.03em"
  body:
    fontFamily: "-apple-system, BlinkMacSystemFont, SF Pro Display, PingFang SC, Noto Sans SC, sans-serif"
    lineHeight: 1.5
  label:
    fontFamily: "Bahnschrift Condensed, Arial Narrow, sans-serif"
    fontWeight: 800
    lineHeight: 1
rounded:
  none: "0"
  instrument: "50%"
spacing:
  base: "8px"
  screen-x: "20px"
  tap-target: "44px"
  bottom-nav: "74px"
components:
  action-chamber:
    backgroundColor: "{colors.chamber}"
    textColor: "{colors.text-on-dark}"
    rounded: "{rounded.none}"
    padding: "22px 22px 16px"
  signal-action:
    backgroundColor: "{colors.signal}"
    textColor: "{colors.ink}"
    rounded: "{rounded.none}"
    height: "48px"
  archive-task:
    backgroundColor: "{colors.paper-raised}"
    textColor: "{colors.ink}"
    rounded: "{rounded.none}"
    height: "76px"
  bottom-navigation:
    backgroundColor: "{colors.ink}"
    textColor: "{colors.muted-on-dark}"
    rounded: "{rounded.none}"
    height: "74px"
---

# Design System: 序升

## Overview

**Creative North Star: “个人行动档案”**

序升把每一次现实行动记录成角色成长证据。五个页面已经统一在同一个视觉世界中：暖白测绘纸负责信息与留白，炭黑行动舱负责高密度数据和操作，荧光黄只负责当前状态、真实进度、完成与确认。细线刻度、窄体数字、硬切角和装配式分区提供测绘档案的精密感。

统一视觉不等于复制同一种版式。首页是首屏完成闭环的绝对构图海报式角色档案；主线、支线、成就和角色页是适合滚动与连续操作的档案式纵向布局。每页保留自己的职责、信息密度和操作顺序。

**Key Characteristics:**

- 五页共享暖白、炭黑、荧光黄、细线与硬切角，不再存在旧深色页面分支。
- 首页以分层微动态角色和底部行动舱建立“状态 → 成长 → 行动”的首屏叙事。
- 其余四页以固定顶栏、纵向档案内容和固定底部导航承载高频操作与长内容。
- 页面差异来自职责和内容结构，不来自重新发明颜色或组件语言。

## Colors

主色关系固定为“纸面承载、炭黑聚焦、黄色情号”。高亮必须稀缺，不能把整页染成黄色或以其他高饱和色争夺语义。

### Primary

- **测绘纸白** (`#ececeb`)：五页画布、角色展示区与大面积负空间。
- **信号荧光黄** (`#e8f300`)：当前导航、进度填充、完成态、主要确认、关键数字与短促奖励反馈。

### Secondary

- **纸面抬升层** (`#f5f5f2`)：任务行和需要从纸面轻微分离的操作单元。
- **炭黑行动舱** (`#17191a`)：首页行动区、支线分类带、角色数据舱、弹窗与高密度操作区域。
- **炭黑次级面** (`#222425`)：行动舱内部模块和数据舱的次级分层。

### Tertiary

- **风险红** (`#ff6b67`)：仅用于不可逆或危险状态；不参与常规成长、奖励和分类编码。

### Neutral

- **炭黑墨色** (`#111214`)：纸面正文、边框、图标和底部导航。
- **深色面正文** (`#f1f1ed`)：行动舱与数据舱的主要文字。
- **纸面次文** (`#626463`) / **深色面次文** (`#a9abaa`)：说明、时间、单位和辅助状态。
- **档案分隔线** (`#bfc0bd`)：纸面列表、模块和数据关系线；深色面改用低透明白线。

### Named Rules

**The One Signal Rule.** 荧光黄是全站唯一常规强调色；蓝、青、紫等颜色不得重新承担完成、选中或主要操作语义。

**The Rare Signal Rule.** 黄色必须对应当前状态、真实进度、完成、解锁或可执行确认；没有状态或行为含义的黄色应移除。

**The Surface Role Rule.** 纸白负责阅读与扫描，炭黑负责聚焦与行动；不得用大面积灰色卡片把两者稀释成通用后台界面。

## Typography

**Display Font:** Bahnschrift Condensed（回退 Arial Narrow、sans-serif）

**Body Font:** 系统无衬线（Apple / BlinkMacSystemFont / SF Pro Display / 苹方 / Noto Sans SC）

**Label/Measurement Font:** Bahnschrift Condensed；数值启用等宽数字特性。

**Character:** 窄体重字像档案编号和设备铭牌，负责标题、等级与关键计数；系统正文字体负责中文任务、说明和反馈。层级依靠尺度、字重、明暗与紧凑间距，不靠装饰性眉题堆叠。

### Hierarchy

- **首页品牌显示字**（900，38px，0.95）：仅作左上品牌锚点。
- **首页等级数字**（900，94px，0.82）：首页第一数据层，紧字距、等宽数字。
- **档案页标题**（900，34–36px）：主线、支线、成就和角色页的页面入口。
- **区块标题**（900，20–31px）：首页行动舱与档案页章节标题。
- **正文**（系统常规，11–14px）：任务、说明和记录，优先保证快速扫读。
- **标签 / 测量值**（7–17px）：日期、XP、百分比、计数、单位和设备式标记；英文可大写，中文不做强制拉宽。

### Named Rules

**The Measurement Hierarchy Rule.** 最大字号只给等级、进度、页面标题和关键行动标题；普通说明不得借用海报级字号。

**The Numeric Evidence Rule.** XP、等级、百分比和计数使用稳定的等宽数字，让变化看起来像可核对的成长记录。

## Layout

产品以 iPhone 竖屏为基准，内容最大宽度 520px，基础节奏 8px，档案页左右边距 20px，点击目标不小于 44px。底部五项导航高 74px并叠加安全区；档案页主内容为顶栏预留 76px、底部预留约 96px，保证滚动内容不被固定区域遮挡。

**首页：海报式绝对构图。** 首页占据扣除底部导航后的首屏，不依赖滚动完成核心闭环。阅读顺序是左上品牌与日期 → 今日状态 → 左中等级与进度 → 右侧分层角色 → 底部炭黑行动舱。角色、测量线和状态采用有控制的绝对定位；行动舱位于拇指区。低高度设备只压缩人物和舱体尺寸，不改变叙事顺序。

**主线：任务执行档案。** 纵向滚动，页首用标题、完成分数和总进度建立全局状态，随后按任务组连续排列 76px 任务行；完成态直接反转为荧光黄。

**支线：探索记录档案。** 纵向滚动，页首保留新增入口；炭黑三列分类带总结阅读、观影和学习，下面用轻量时间线承载最近记录。没有逾期语义，不复制主线的任务压力。

**成就：里程碑档案。** 纵向滚动，黄色焦点块呈现最接近的成就，后接里程碑列表和现实奖励协议；视觉顺序从“正在接近”到“累计证据”再到“兑现”。

**角色：全身成长档案。** 页面上部为完整人物展示，下部进入炭黑数据舱。角色页使用一体化静态素材 `assets/live2d/profile_character_integrated_v2.png`；在 390×844 视口中必须从狼耳到双脚完整显示，不得裁成半身或重新叠加首页头颈图层。等级、称号与四维属性采用边缘扫描 HUD 围绕人物，短引导线避开脸部、狼耳和双脚；减脂、统计和报告进入可滚动数据舱。

## Elevation & Depth

系统以平面叠层和明度切换为主。测绘纸、抬升纸面、炭黑舱体和荧光黄信号通过遮挡、边框、切角和局部明暗建立结构；常态不用泛光或悬浮卡片阴影。首页角色使用 `0 12px 8px rgba(15,17,17,.12)` 的克制下落投影，行动舱使用 `0 -8px 22px rgba(18,20,20,.14)` 的向上抬升阴影。

**The Flat Archive Rule.** 常态表面必须像印刷和装配出来的档案；阴影只解释角色与纸面、行动舱与拇指区的层级，不作为装饰。

## Shapes

主形态为零圆角、1px 细线、斜切角、折线开口和非对称多边形。任务行右上切 12px，首页行动舱使用折线舱口，主要行动使用单角切口；身份标记、添加按钮和报告按钮保持机械式切角。圆形只用于确有轨道、节点或状态灯含义的仪表元素，不能扩张为胶囊、圆角卡片或装饰气泡。

## Components

### 底部导航

- 五页统一使用炭黑底与五等分布局，未选中项为灰字，当前项以完整荧光黄矩形反显并转为炭黑图标和文字。
- 图标约 24px，使用方形端点和硬转角；导航不再按页面切换不同配色系统。

### 首页角色舞台与行动舱

- 首页角色由对齐的身体、颈部、无耳头颅、左右狼耳、眨眼补丁与前景衣领分层构成；这是首页专属微动态结构，不用角色页全身静态图替换。
- 行动舱为炭黑硬切角容器；下一项行动使用 84px 标识区、弹性内容区和 105px 黄色确认区，总高 92px。
- 确认区只承载立即行动；“查看全部行动”是次级入口，不得与确认区同权重。

### 档案任务行

- 默认使用抬升纸白、1px 灰线和右上 12px 切角，最小高度 76px。
- 左侧 44px 标识、中部任务说明、右侧 XP 证据形成稳定三列；完成后整行变黄，标识反转为炭黑。

### 炭黑数据舱

- 用于角色属性、减脂、统计、支线分类与弹窗；内部以低透明线、深浅炭黑和黄色关键值分层。
- 不是通用卡片皮肤：只有需要高密度阅读或集中操作的区域才能进入数据舱。

### 成就与奖励

- 当前最接近的成就使用整块黄色焦点面；普通里程碑保持纸面列表。
- 已获得徽章与已解锁奖励使用炭黑 / 黄色反转；未获得状态只用线框和灰色说明。

### 角色全身舞台

- 主素材为 `assets/live2d/profile_character_integrated_v2.png`，`object-fit: contain`，以完整轮廓和自然头颈比例优先于人物放大。
- 身份与称号位于左上，等级位于左下，四维属性以左右各两枚的非对称扫描节点贴近肩臂与腰腿，垂直舞台标记位于右侧；所有覆盖信息避开狼耳、面部、手部和双脚。

### 表单与主要操作

- 弹窗与高密度表单使用炭黑舱体、零圆角底部描边输入；聚焦转为荧光黄边线或 2px 可见轮廓。
- 主要操作按钮为荧光黄底、炭黑字，最小高度 48px；危险操作保留风险红语义但不得与主操作并列争抢。

### Motion

- 页面进入使用约 360ms 的轻微横移、淡入和模糊复位；进度变化使用 500–800ms 与 `cubic-bezier(.16,1,.3,1)`。
- 首页角色保留 4.8s 低幅呼吸、克制头部微摆、随机 3.5–7.5s 眨眼、随机 4.8–11.8s 单耳轻抖；点击以单耳回应为主，任务完成附加约 900ms 黄色能量光晕。
- 角色页全身立绘保持静态完整展示，避免与首页角色抢夺动态职责。
- `prefers-reduced-motion: reduce` 下关闭循环角色动画，并把过渡压缩为近乎即时；功能和状态变化仍必须可见。

## Do's and Don'ts

### Do:

- **Do** 在五页统一使用暖白测绘纸、炭黑行动舱、荧光黄信号、细线刻度与硬切角。
- **Do** 让首页在首屏完成“状态 → 角色成长 → 下一项行动”，并把直接操作留在拇指区。
- **Do** 让主线、支线、成就和角色页使用可滚动档案布局，保留各自的操作职责。
- **Do** 在 390×844 角色页完整展示 `profile_character_integrated_v2.png` 的狼耳到双脚，并为覆盖信息保留轮廓安全区。
- **Do** 把首页分层微动态角色和角色页全身成长立绘视为同一人物的两种明确职责。

### Don't:

- **Don't** 再把主线、支线、成就或角色页描述为旧深色系统；五页视觉迁移已经完成。
- **Don't** 把首页绝对定位海报构图复制到需要滚动、录入和连续扫描的档案页。
- **Don't** 用旧能量青、分类彩虹或多种高饱和色重新分割全站语义；常规强调只有荧光黄。
- **Don't** 使用圆角卡片网格、胶囊、柔和渐变堆叠、玻璃拟态或常态霓虹发光。
- **Don't** 让状态、等级或数据面板遮挡角色面部、狼耳、手部、双脚及服装关键结构。
- **Don't** 给静态信息添加无意义循环动画，或在减少动态偏好下保留呼吸、眨眼和耳动。
